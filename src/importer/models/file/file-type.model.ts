// enum AceptedExtensions {
//   Csv = 'csv'
// }
// export type FileExtension = `${ string | number }.${ AceptedExtensions }`;      //ts-Jest see it like an error but typescript accept it
export type FileExtension = string;
